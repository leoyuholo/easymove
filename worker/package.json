{
  "name": "easymove-coding-test-worker",
  "description": "worker for computing shortest driving distance",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "mocha -t 10000 --recursive",
    "test-watch": "nodemon --exec npm run test --watch . --ignore */nockFixtures"
  },
  "dependencies": {
    "@google/maps": "^0.4.3",
    "config": "^1.26.1",
    "lodash": "^4.17.4"
  },
  "devDependencies": {
    "chai": "^4.1.0",
    "chai-as-promised": "^7.1.1",
    "mocha": "^3.5.0",
    "nock": "^9.0.14",
    "nodemon": "^1.11.0"
  }
}
